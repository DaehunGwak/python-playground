# x_project

## django commands

```shell
python manage.py startapp <app_label>

python manage.py migrate # 기본 django 앱들 마이그레이션
python manage.py createsuperuser # root / roottest

python manage.py makemigrations <app_label>
python manage.py migrate <app_label:optional> <migration_name:optional>
```